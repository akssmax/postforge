/**
 * Export golden landing designs to public/landing/golden/*.png
 *
 * Run from the app after mounting golden designs on a hidden export route,
 * or export manually from /tool and save to public/landing/golden/.
 *
 * Usage (future): node scripts/export-landing-golden.mjs
 */
console.log(
  "Golden PNG export: use /tool to export designs, or wire capturePostImage on a dev route.",
);
